# serve-with-fastify

This shows how to use the `jetpack/serve` middleware in development and production when using fastify.

    $ npm install
    $ npm run watch
    $ npm run build
    $ npm start
